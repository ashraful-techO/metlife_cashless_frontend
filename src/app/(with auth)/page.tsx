import { AppoinmentUnit } from "@/app/_components";

export const metadata = {
	title: {
		template: "Dashboard | Zaynax Health",
		default: "Dashboard | Zaynax Health",
	},
};
const Dashboard = () => {
	return <AppoinmentUnit />;
};

export default Dashboard;
